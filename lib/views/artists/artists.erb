<html>
  <body>
    <h1> Artists - <%= Artist.all.size %> Total</h1>
    <ol>
      <% Artist.all.sort_by {|a| a.name}.each do |artist, i| %>
      <li><a href="#"><%= "#{artist.name} - #{artist.songs.count} songs" %></a></li>
      <% end %>
    </ol>
  </body>
</html>